const nombre = "Lopez";
const apellido = "maricarmen";

//console.log( `El nombre del pasajero es: ${nombre} ${apellido}`);
let nombreCompelto = `El nombre del pasajero es: ${nombre} ${apellido}`;
/**
 * Usando `` comillas ivertidas seguido de ${VARIABLE} podemos interlar 
 * cadenas de caracteres sin que tengamos que usar la manera tradicionar 
 * del  + variable y esas cosas
 * 
 */